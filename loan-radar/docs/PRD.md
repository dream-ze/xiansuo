# PRD

## 作用

记录智获客 Demo 可成交版的产品目标、核心范围、阶段边界和验收口径。

## 当前阶段

步骤 0：项目骨架和文档。

本阶段不实现业务功能，只为后续 P0 后端主链路提供文档入口。

## 范围边界

当前优先跑通后端主链路：

监控源管理 -> 采集任务 -> Mock 采集 -> 帖子池 -> 评论池 -> 线索识别 -> 线索池 -> 今日获客报告 -> CSV 导出。

暂缓 CRM、员工管理、自动发布、真实平台采集和智能体编排。
